version: '3'
services:
  api:
    build: .
    user: node
    env_file: .env
    ports:
      - "3000:3000"
    depends_on:
      - db

  db:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
    networks:
      - backend

networks:
  backend:
    driver: bridge
